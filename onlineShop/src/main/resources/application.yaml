spring:
  application:
    name: sportshop
  datasource:
    url: jdbc:postgresql://localhost:5432/sportshop
    password: alexa
    username: myshop
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

